@model IEnumerable<Library.Models.kutuphane>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<br />
<input type="button" class="btn btn-outline-warning" data-mdb-ripple-color="dark" value="Kutuphane Ekle" onclick="location.href='@Url.Action("Create", "Kutuphane" )'" />
<br />
<br />
<table class="table">
    <tr class="table-success">
        <th>
            @Html.DisplayNameFor(model => model.kutuphane_adi)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr class="table-info">
            <td>
                @Html.DisplayFor(modelItem => item.kutuphane_adi)
            </td>
            <td>
                <input type="button" class="btn btn-success btn-rounded" value="Duzenle" onclick="location.href='@Url.Action("Edit", "Kutuphane" , new { id = item.kutuphane_id })'" />
                <input type="button" class="btn btn-danger btn-rounded" value="Sil" onclick="location.href='@Url.Action("Delete", "Kutuphane" , new { id = item.kutuphane_id })'" />
            </td>
        </tr>
    }

</table>
